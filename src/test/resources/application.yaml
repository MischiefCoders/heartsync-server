spring:
  datasource:
    url:
    username: user
    password: pw
    driver-class-name: org.h2.Driver

  jpa:
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.hibernate.type.descriptor.sql: trace
